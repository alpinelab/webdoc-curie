.modal.fade{id: "#{name}-modal"}
  .modal-dialog.modal-lg
    .modal-content
      .modal-header
        %button.close{"data-dismiss" => "modal", :type => "button"}
          %span{"aria-hidden" => "true"} ×
          %span.sr-only= I18n.t("common.close")
        %h1.modal-title= I18n.t("#{name}.title")
      .modal-body
        = partial("pages/#{name}/#{name}")
